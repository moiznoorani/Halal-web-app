{
  "name": "Halal Restaurant Finder",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",

  "forwardPorts": [3000, 3001, 5432],
  "portsAttributes": {
    "3000": { "label": "React Frontend", "onAutoForward": "openBrowser" },
    "3001": { "label": "Express API",    "onAutoForward": "silent" },
    "5432": { "label": "PostgreSQL",     "onAutoForward": "silent" }
  },

  // Runs ONCE when the codespace is first created
  "postCreateCommand": "bash /workspace/.devcontainer/install.sh",

  // Runs every time the codespace starts (including after suspend)
  "postStartCommand": "bash /workspace/.devcontainer/start-servers.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode"
      }
    }
  }
}
